# Cloakscreen Environment Variables
# Copy this file to .env and fill in your actual credentials

# DRM Provider Configuration
# Get these values from your DRM provider console
DRM_PROVIDER=pallycon
DRM_SITE_ID=your_site_id
DRM_SITE_KEY=your_site_key
DRM_ACCESS_KEY=your_access_key
DRM_KMS_TOKEN=your_kms_token
DRM_TOKEN_ENDPOINT=/api/get-license-token

# Optional: Override provider defaults (auto-detected if not specified)
# DRM_LICENSE_SERVER=https://custom-license-server.com
# DRM_CERTIFICATE_URI=https://custom-certificate-server.com

# Provider-specific debug options (varies by provider)
# DRM_DEBUG=true

# Security Note:
# - Never commit .env files to version control
# - Use different credentials for development/staging/production
# - Rotate API keys regularly